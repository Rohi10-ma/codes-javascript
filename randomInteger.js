//Random Integer from 1 to 10

//Get a random decimal usiong Math.random() and multiply it with 10

var decimal = Math.random() *10;

//Now round of to the nearest integer using Math.ceil()

var integer = Math.ceil(decimal);

console.log(integer); // will give a random integer every time the code is run!